# AGENTS.md

给在本仓库里干活的 coding agent 看。人读的介绍在 `README.md`；两者冲突时，以代码和测试的实际行为为准，并把过时的那份改掉。

## 这是什么

STM32 课程设计「万年历时钟」。两人分工：A 负责算法、状态机与软件框架，B 负责驱动与硬件。评分按功能逐项上板验收，每项 5 分，所以「某项功能在真实硬件上可演示」是唯一的完成标准，host 测试通过只是前置条件。

**当前状态**：开发板型号未定，仓库里只有与芯片无关的部分。没有 CubeMX/CubeIDE 工程，没有任何驱动，没有一项功能上过板。不要在文档、提交信息或回复里把任何功能写成「已验收」「已在硬件上验证」。

## 结构与不变量

```
core/include/mc/  core/src/   纯 C11 算法与状态机
app/port.h                    硬件边界（六个函数）
app/app.c  app/app.h          应用层骨架，只经 port.h 碰硬件
tests/                        host 单元测试；test_app.c 自带假 port 与脚本化 ESP8266
tools/                        lunar_dump.c + verify_lunar.ps1：农历全范围核对
scripts/host-test.ps1         配置 + 编译 + 跑测试
```

必须守住的几条，破坏任何一条都要先停下来问人：

1. **`core/` 不得包含任何 HAL、CMSIS 或芯片头文件**，不得读时钟、不得碰外设。需要时间的函数一律由调用者传入 `now_ms`。这是它能在 PC 上测、换板不用改的前提。
2. **`app/` 只通过 `port.h` 碰硬件。** 需要新的硬件能力就往 `port.h` 加函数，并同步更新 `tests/test_app.c` 里的假实现；不要在 `app/` 里直接 include 驱动头文件。`port.h` 是两位成员代码的接缝，改它的签名等于改对方的接口，要在提交信息里写明。
3. **中断分层**：中断里只累加毫秒时基、把串口字节放进环形缓冲、给外部触发抓时间戳。解析、状态机、I²C、刷屏、DHT11 一律在主循环（`app_poll()`）。`core/` 的模块都不可重入，不要从中断里调用。
4. **不许阻塞。** `app_poll()` 及其调用的一切不得有 `HAL_Delay`、忙等或阻塞式串口读。超时用「期限 + 轮询」实现，参照 `mc_sntp.c` 的 `deadline_passed()`。
5. **毫秒时基是会回绕的 `uint32_t`。** 时间差只用无符号减法 `now - start`，期限比较只用 `(int32_t)(now - deadline) >= 0`；不要写 `now >= deadline`。每个涉及时间的模块都有回绕测试，新增的也要有。
6. **不动态分配内存**，不用变长数组，缓冲区大小用宏写死并检查越界。
7. `mc_lunar.c` 的 `k_lunar_info` 表已与独立实现逐日核对过，**不要手改其中任何常量**；若确需改动，必须重跑 `tools\verify_lunar.ps1` 并在提交信息里贴结果。

## 编译与测试

Windows，PATH 里不需要有 cmake / ninja / cl，脚本借用 Visual Studio 自带的工具链：

```powershell
.\scripts\host-test.ps1      # 必跑。/W4 /WX，告警即失败
.\tools\verify_lunar.ps1     # 动过 mc_lunar.c 或 mc_date.c 时必跑，约一分钟
```

`host-test.ps1` 输出开头可能有一行 `'vswhere.exe' is not recognized…`，来自 VS 开发者环境脚本自身，无害，忽略。

非 Windows 或已有工具链时：`cmake --preset host` 需要 `cl`；用 gcc/clang 就直接 `cmake -S . -B build/gcc -G Ninja && cmake --build build/gcc && ctest --test-dir build/gcc`，此时编译选项是 `-Wall -Wextra -Wpedantic -Wconversion -Werror`。gcc/clang 这条路径目前没人实际跑过，首次使用若有告警，修代码，不要放宽选项。

改完代码的完成标准：上述测试实际跑过且全过。没跑就说没跑，不要凭推断报告「测试通过」。

## 写代码的约定

- C11，无编译器扩展。公开符号前缀 `mc_`（core）、`app_`、`port_`；文件内部函数与表一律 `static`。
- 源文件 UTF-8（MSVC 靠 `/utf-8`）。注释用中文，只写「为什么」和失效机理，不复述代码。
- 失败用返回值表达（`bool` 或状态枚举），不用全局错误码，不用断言代替输入检查。对外部输入（串口应答、用户设置的日期）一律先校验再用。
- 测试用 `tests/check.h` 里的 `CHECK` / `CHECK_EQ`，每个测试文件一个 `main`，在 `CMakeLists.txt` 里登记。不要引入第三方测试框架或任何外部依赖。
- 新功能先写成 `core/` 里可在 host 上测的纯逻辑，再经 `port.h` 接硬件；「只能上板才能测」的逻辑尽量压到最薄。
- 最小改动：只改任务要求的范围，不顺手重构、不改风格、不重排无关代码。
- 测试里的期望值要有独立来源（标准库、另一实现、可人工核对的锚点），不要把被测代码自己的输出抄回去当期望值。

## 不要做的事

- 不要提交 `app/app_config.h`（含热点密码，已被 `.gitignore` 挡住）；不要把任何真实凭据、姓名、学号写进仓库——这是公开仓库。
- 不要提交 `build/`。
- 不要猜引脚、芯片型号、外设编号或屏的型号。这些目前未定；需要时问人，或写成 `port.h` 后面的待实现项。
- 不要为了让测试通过而删测试、放宽断言或关告警。
- 不要在没有被要求时 push、改远程、改分支结构。

## 未完成项（截至 2026-09-20）

| 项 | 归属 | 说明 |
|---|---|---|
| 闹钟（按 RTC 时刻触发）、番茄钟（25/5 循环） | B（基本功能 3） | 未实现。应写成 `core/` 里收 `now_ms` 或当前时间的状态机。单路秒表与定时可直接用 `mc_timers` |
| 时间设置的编辑逻辑、整点报时判定 | B（基本功能 2） | 未实现。字段进位与合法性可复用 `mc_date.h` |
| DS3231、DHT11、LCD、按键、蜂鸣器驱动；`port.h` 的真实实现 | B | 等开发板型号 |
| 外部触发计时 | B（扩展功能 4） | 接法见 `README.md`「给 B」一节 |
| 各功能的显示与按键界面 | 未分 | 等屏的型号。农历名称表是 UTF-8，GB2312 字库的屏要在显示层转码 |
| ESP8266 真机联调 | A | `mc_sntp` 只对脚本化应答测过。不同 AT 固件的 `CIPSNTPCFG` 时区参数写法有差异，联调前先用串口助手手敲一遍 |
| 农历 1900-01-01 至 1901-02-18 | — | 超出核对用参照实现的范围，仅有连续性检查与基准锚点，无独立逐日比对 |
| CubeIDE 固件工程 | A | 等开发板型号。建在本仓库内，路径保持纯 ASCII |
